services:
  - type: web
    name: gestao-estoque-api
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT 'app.wsgi:app'"
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres:senaiads@db.gldaarxfglefflnmhkrk.supabase.co:5432/postgres"
      - key: FLASK_ENV
        value: production