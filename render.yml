services:
  - type: web
    name: gestao-estoque-api
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    # Remova a linha 'startCommand'
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres.gldaarxfglefflnmhkrk:senaiads@aws-1-sa-east-1.pooler.supabase.com:6543/postgres"
      - key: FLASK_ENV
        value: production